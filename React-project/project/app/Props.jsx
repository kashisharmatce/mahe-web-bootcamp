//Props in React.Js:to access Property in multiple comp.
//Map function in javascript : .map()
// iterate over array, change the items of array.
// it doesnt orig. array instead returns a new array
// syntax :  map((value , index ) =>{ logic })
// let Colors = ["red", "blue", "yellow", "green"];

// Colors.map((color) => {
//   console.log("my fav color is" + color);
// });
// let Productcard = [
//   { id: "001", name: "Kashi" },
//   { id: "002", name: "Khushi" },
// ];

// let ProductInfo = Productcard.map(product => ({
//   productid: product.id,
//   productName: product.name
// }));
// <h2>{product.productName}</h2> 
// <h3>{product.productid}</h3>
const Props = ({ product }) => {
  return <div>
    
  </div>;
};

export default Props;
